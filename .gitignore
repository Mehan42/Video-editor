.harness/
.pagevideo/
output/
*.tmp
*.part
*.log
.env
.env.*
!.env.example
coverage.out
*.cover
*.prof

# Local Go caches when system caches are unavailable.
.gocache/
.gomodcache/
.codegraph/

# External local runtime dependencies; keep them outside the application commit.
ffmpeg/
whisper.cpp/

# Pre-built CLI binaries; source of truth is Go code, rebuild: go build -o bin/pagevideo.exe ./cmd/pagevideo.
bin/
